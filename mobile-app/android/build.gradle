buildscript {
    repositories {
        google()
        mavenCentral()
    }
    dependencies {
        // React Native Gradle plugin — version must match your RN version in package.json
        classpath("com.facebook.react:react-native-gradle-plugin:0.73.0")

        // Android Gradle plugin
        classpath("com.android.tools.build:gradle:8.0.2")
    }
}

allprojects {
    repositories {
        google()
        mavenCentral()
    }
}
